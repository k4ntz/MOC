env_name: 'PongDeterministic-v4'

device: 'cuda:0'
parallel: False

logdir: '/xrl/logs/'

video_steps: 100

train:
  gamma: 0.9
  learning_rate: 0.001

  num_episodes: 50000
  max_steps: 100000
  
  log_steps: 500
  # save every is for episodes
  save_every: 100


